[theme]
primaryColor = "#7C3AED"
backgroundColor = "#F8FAFC"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#1E293B"
font = "sans serif"

[server]
runOnSave = true
# Disable CORS if running locally to avoid IP issues, 
# but allow XSRF protection to remain default or disable it if blocking.
enableCORS = false
enableXsrfProtection = false
